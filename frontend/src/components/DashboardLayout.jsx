import React from 'react'


const DashboardLayout = ({children, activeMenu}) => {
  return (
    <div>
        {user && (
            <div>
                
            </div>
        )}
    </div>
  )
}

export default DashboardLayout